export type Character = {
  name: string;
  image: string;
  status: string;
  origin: {
    name: string;
  };
  location: {
    name: string;
  };
  species: string;
  gender: string;
};
